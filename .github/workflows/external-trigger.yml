# name: External API Trigger
# on:
#   repository_dispatch:
#     types: [deploy-request]

# jobs:
#   handle-dispatch:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Process External Payload
#         run: |
#           echo "Received request to deploy to environment: ${{ github.event.client_payload.environment }}"
